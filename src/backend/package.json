{
  "name": "webrtc-android",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --hot run src/index.ts",
    "build": "bun run ./build.ts",
    "start": "bun dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/cookie": "^0.6.0",
    "cookie": "^0.6.0"
  }
}
